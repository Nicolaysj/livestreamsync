# Normalize line endings to LF in the repo (checked out native on each OS).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.ico binary
*.icns binary
*.mp4 binary
*.mov binary
*.exe binary
